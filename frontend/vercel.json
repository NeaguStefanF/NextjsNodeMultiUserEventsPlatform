{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "commands": {
    "build": "npm install && npm i --force react-render-html && npm install --force --legacy-peer-deps"
  }
}
